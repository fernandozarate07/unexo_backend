# ⚠️ RECORDATORIO IMPORTANTE ⚠️
# Actualmente estás usando el usuario ROOT para conectar a MySQL.
# El usuario 'root' tiene permisos totales en todo el sistema de bases de datos,
# lo cual es útil para desarrollo local pero ES PELIGROSO en producción.
# 
# Antes de subir la app a producción:
# 🔐 Crear un usuario nuevo solo para esta app (por ejemplo 'unexo_user')
# con una contraseña segura y permisos restringidos solo sobre la base de datos 'unexo'.
# Ejemplo de cómo crearlo:
#   CREATE USER 'unexo_user'@'localhost' IDENTIFIED BY 'password_segura';
#   GRANT ALL PRIVILEGES ON unexo.* TO 'unexo_user'@'localhost';
#   FLUSH PRIVILEGES;

# Base de Datos
DATABASE_URL="mysql://root@localhost:3306/unexo"
